import { QuestionsearchPipe } from './questionsearch.pipe';

describe('QuestionsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
